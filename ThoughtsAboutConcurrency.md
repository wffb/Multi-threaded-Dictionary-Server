### 关于并发的一些思考

在本项目中为了做出具有良好的并发性和可靠的程序，主要做出了三个设计

- NIO Socket进行连接管理
- WorkerPooExecutor 进行任务线程的管理
- ConcurrentHashMap进行系统内容的管理



#### ConcurrentHashMap 进行系统内容的管理

##### 采用怎样的数据结构

本项目为字典服务器项目，因此频率最高的操作应当是 search 搜索操作。因此采用Map结构存储 `< word, meaning>` 信息，来降低搜索时间复杂度



##### 如何控制并发安全

我首先想到采用的是 `ConcurrentHashMap` , 它提供了安全的 `get, put, delete`功能。但是初次之外我还需自行设计线程安全的：

- updateMeaning: 更新词义
- addMeaning: 添加词义

我基于 **CAS** 的原则，用`ConcurrentHashMap`的`get , put`尝试完成设计



##### 与并发安全相符的数据设计

一个单词可能对应多个含义，如何存储呢？ 我首先想到用List 即以 `Map<String , List<String>>`。如果是这样的话，在进行上述两个CAS操作时就需要：

- 对当前List 进行深拷贝
- 修改操作
- 对新旧List 进行遍历对比（根据List的equal())

可以看到相当笨重，并且有违使用习惯，因此我转为使用 <u>一个整个字符串 存储所有意思并用 特定字符进行分割</u>，这样的设计我认为具有以下好处：

- 单义修改转为了对整个字符串的替换，不需要考虑深拷贝问题 和 List修改中被读取的问题 ， 更加符合CAS的精神
- 字符串传送的数据量较小

但是也加重了客户端的解析工作